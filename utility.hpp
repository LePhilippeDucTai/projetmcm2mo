#include <vector>
#include <iostream>
#include <cmath>

// This file contains utility functions such as range, display_vect, ...

std::vector<double> range(double a, double b, double by);
template<typename T> void display_vect(std::vector<T> t);

